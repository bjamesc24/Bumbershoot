The purpose of the "wp/endpoints/" folder is for documentation. No real code is to be executed here but will explain
each endpoint and its expected behavior/output. the .YAML file is meant for demonstration purposes. Upload it to a site like
swagger.io and see how each endpoint is supposted to interact with the app. 
